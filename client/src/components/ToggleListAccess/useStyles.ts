import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  root: {
    marginTop: '20px',
  },
  label: {
    fontSize: 15,
    paddingLeft: '5px',
    fontWeight: 'bolder',
  },
  marginAuto: {
    margin: 'auto',
  },
}));

export default useStyles;
